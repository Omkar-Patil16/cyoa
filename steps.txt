write a go struct for the json data use mhot site in description 
create story.go with package cyoa and paste the struct
create option struct
create story type map of string to chapter
create a cmd and inside dir cyoaweb which hold our main.go  this will run our main web application

now we want to parse the json file to story struct
  - we create a flag to pass the file and print the filename getting used
  - Open the file also look for error
  - create a instance of the Jsonfile decoder which takes io.Reader
  - decode the file to var created from story package, look for error and print the var
  - 


// refactoring

- create a func of json parser in story.go

// add html template and star rendering story

- add the html template as var in main.go

// create a handler to render the template
// we are unsing the handler interface

- create a handler func which is returns http.Hanlder 
- create a handler struct which will be returned in previous function
- create func ServeHttp with your hanlder 
- create a template by using func init 
- create a global var as pointer to template
- excute the template by passing some data that is story
- create a port flag


31-07-24

// parse the path to call a perticular story

- get the path from quest 
- check if path is empty or / go to start of the story to intro
- slice the path to remove the slash 
- check if the path exist in the map
  - if it exist and there is error handle and log the error
  - else only return
  - if path not found http.Error



// create a path function 
- create a function that retruns handler option
- create a path function that parse new path
- updated the htmp template 